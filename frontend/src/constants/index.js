import baseUrl from "./urls";

export const USER_LOGIN = "USER_LOGIN";

export const authentication = `${baseUrl}backend/api/auth/token/`;

export const registration = `${baseUrl}backend/api/registration/`

export const validation = `${baseUrl}backend/api/registration/validate/`

export const userprofileurl = `${baseUrl}backend/api/users/me/`

export const USER_TOKEN = "USER_TOKEN"

export const listsRestaurants = `${baseUrl}backend/api/restaurants/`

export const List_all_Restaurants = "List_all_Restaurants"

export const ABOUT_USER = "ABOUT_USER"

export const createRestaurant = `${baseUrl}backend/api/restaurants/new/`

export const USER_LOGOUT = "USER_LOGOUT";